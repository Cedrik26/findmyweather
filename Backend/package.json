{
  "name": "backend",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "set NODE_PATH=./node_modules && bun --watch src/index.ts",
    "start": "set NODE_PATH=./node_modules && bun src/index.ts",
    "test": "bun test",
    "docker:build": "docker build -t ghcn-weather-backend .",
    "docker:run": "docker run -p 3000:3000 -v ./data:/app/data ghcn-weather-backend"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0",
    "@types/cors": "^2.8.17"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.0.1",
    "node-cache": "^5.1.2",
    "cors": "^2.8.5",
    "zod": "^3.24.1"
  }
}